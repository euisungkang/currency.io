import * as convert from '$lib/server/convert.js';

export function load() {
    let metadata = convert.ge
}

export const actions = {
    convert: async ({ request }) => {
        console.log(await request.formData());
    }
}
